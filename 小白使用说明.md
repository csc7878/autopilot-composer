# AutoPilot Composer 小白使用说明书（v3.6.0）

> 不会写代码也能用：把"打开网页 → 填表 → 点击 → 截图"这类重复活儿写成一张清单，让它自动跑。更省事的是——**让它"看一遍"你的操作，它就自动学会了**，连清单都不用写。

> 📌 **本文档随 v3.6.0 发布更新**：新增 **"录的时候顺手帮你把检查点写好"**——以前"检查点"（第 8 节那个）要你自己一行行手写，现在**录制时它自己就记下来了**：点完按钮页面跳到了哪、输入的内容有没有真的填进去、有没有弹出"保存成功"……它一边看你操作一边把这些写成检查规则，不用你操心。录完会多出一份 `verify_candidates.md`，逐条告诉你"我加了什么、为什么加、什么没加、为什么没加"。**默认只加最可靠的那几种**，不会乱加导致误报。详见第 10 节。
> 📌 **同时修了一个很坑的连接问题**：如果你电脑上装了代理软件（Clash 之类），以前会出现"Chrome 明明开着，它却说连不上、报 502"，现在修好了，不用管了。
>
> 📌 **（上一版 v3.5.0）**：新增 **"它会自己琢磨这一步有没有把握"**——机器人会给每一步打一个 0~5 分的"把握分"，分数低就说明这步不太靠谱；没把握时它能**主动停下来问你**，而不是闭着眼睛一路错到底。它还会识别"被踢回登录页、跳到错误页、冒出弹窗"这类**环境陷阱**，重试时也不再傻乎乎重复同一个动作、而是换一种做法。**默认是"只看不拦"的观察模式，绝不会打扰你**。详见第 9 节，完整教程见仓库 `docs/confidence-and-intervention.md`。
>
> 📌 **（上一版 v3.4.1）**：在 v3.4.0（T1 直连层：机器人能直接调接口、查数据库，不再傻傻点界面）基础上，新增**两项"更稳"能力**——① **结果校验**：关键步骤可以设"检查点"，干完自己核对是否真生效（以前点了按钮就当成功，页面弹错误它也往下跑）；② **元素库自动体检**：记住哪个定位写法好用，连续出错自动弃用，界面改回来又自动复活，用 `python main_task.py --health` 可查看。详见第 8 节，完整教程见仓库 `docs/verify-and-health.md`。

## 1. 它到底是什么？

一句话：**一个"机器人助手"**。你给它一张**任务清单**（写在一个 `task_flow.json` 文件里），它就会按顺序帮你：

- 打开浏览器网页、自动填表、点按钮、截个图留证据；
- 操作电脑桌面软件（打开程序、点图标、按快捷键）；
- 任务很长也不怕——**中途崩了能从断点继续**，不用从头再来。

🤖 打个比方：你雇了个实习生，写个"第一步打开百度、第二步搜关键词、第三步截图"的便签贴他桌上，他就照着做。这工具就是那个"便签 + 实习生"。更进一步：你直接当着他的面操作一遍网页，他边看边记，下次自己就能重复做——这就是下面的"录制模式"。

## 2. 能帮你干啥？

- **批量网页填报**：每天把同样的数据填进同一个后台系统。
- **定时查数据**：打开网站、搜关键词、把结果截图存下来。
- **桌面办公自动化**：打开 ERP → 导出 Excel → 打开网页 → 上传 → 归档。
- **让它"学你一遍"**：在浏览器里正常操作，它自动录成可重复执行的清单（v3.3.0 重点能力）。
- **对话式录制**：用大白话对它说"打开百度搜 XX"，它边做边弹窗跟你确认（最友好）。
- **网页 + 桌面一起录**：软件里复制 → 切到网页填报，一口气录完。
- **夜间跑批**：睡前点一下，让它后台默默把几百条流程跑完。

⚠️ 它**真的会动你的鼠标键盘和浏览器**。只在你自己的电脑、自己的测试账号里用，别拿去动别人的东西。

## 3. 用之前，先准备这 3 样

- [ ] **电脑是 Windows**（这个工具目前主要支持 Windows 桌面）。
- [ ] **装了 Chrome 浏览器**（用来做网页自动化）。
- [ ] **装了 Python 3.10 以上**：到 python.org 下载，安装时记得勾 **Add Python to PATH**。

> 怎么看 Python 装好没？按 `Win+R` → 输入 `cmd` → 回车，在黑窗口里输入 `python --version`，能显示版本号就 OK。

## 4. 5 分钟上手（照着抄就行）—— 回放现成清单

这是**最省事**的方式：你不用会"录制"，只要有一份清单就能让它跑。

### 步骤 1：拿到技能文件夹
把 `autopilot-composer` 文件夹放到一个好找的地方（比如桌面）。进去后有个 `scripts` 子文件夹，里面有 `main_task.py` 等文件。

### 步骤 2：装一次依赖（只需装一次）
打开 `scripts` 文件夹，在地址栏输入 `cmd` 回车，黑窗口里**逐行**粘贴：

```bat
cd scripts
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

看到一堆 `Successfully installed ...` 就成功了。以后每次用，只要先执行 `.venv\Scripts\activate` 即可。（v3.3.2 起 `requirements.txt` 已改为纯英文注释，Windows 下不会再出现中文乱码导致的安装报错。）

### 步骤 3：用"调试模式"打开 Chrome（最关键！）

有两种方式，**方式 A 最简单**：

**方式 A（推荐，一键拉起）**：在 `scripts` 的 `cmd` 窗口里（已激活 `.venv`）输入：

```bat
python browser_launcher.py --port 9222 --open https://www.baidu.com
```

它会自动找到本机 Chrome 并以调试模式拉起、打开百度。

**方式 B（手动）**：复制整行，在 `cmd` 里粘贴回车：

```bat
"C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --remote-allow-origins=* --user-data-dir="C:\temp\apc_profile" --new-window
```

✅ **验证成功**：浏览器访问 http://127.0.0.1:9222/json/version ，能看到 Chrome 版本信息，说明"遥控通道"已接通。
> 注意：`--remote-allow-origins=*` 一定不能少，少了会报 403 连不上。

### 步骤 4：写你的任务清单 `task_flow.json`
在 `scripts` 文件夹里新建文件，命名为 `task_flow.json`，把下面这段**直接复制进去**保存（这是一个能立刻跑的例子：打开必应 → 搜关键词 → 截图）：

```json
[
  { "type": "browser", "func": "open_url",   "args": ["https://www.bing.com"] },
  { "type": "browser", "func": "input_text", "args": ["#sb_form_q", "AutoPilot Composer"] },
  { "type": "browser", "func": "click_elem", "args": ["#sb_form_go"] },
  { "type": "browser", "func": "screenshot", "args": ["./bing_result.png"] }
]
```

### 步骤 5：一键开跑！
在 `scripts` 的 `cmd` 窗口里（已激活 `.venv`）输入：

```bat
python main_task.py
```

你会看到浏览器自动打开必应、输入文字、点击搜索、然后截一张图保存到 `scripts/bing_result.png`。🎉 恭喜，你跑通了第一个自动化！

## 5. 让它"学你一遍"：录制模式（v3.3.0 重点能力）

不想手写清单？让机器人**看着你操作**，它自动把每一步记成 `task_flow.json`，下次直接回放。

### 5.1 手动录制（recorder.py）

先在调试模式的 Chrome 里打开你要操作的网页（用第 4 节步骤 3 的方式 A/B 打开），然后在 `scripts` 的 `cmd` 窗口里：

```bat
python recorder.py
```

- 回车开始 → 在浏览器里正常操作（点、填、跳转到下一页都行）→ 回到 `cmd` 窗口输入任意内容回车停止。
- 停止后自动生成 `task_flow.json`（可直接用 `main_task.py` 播放）和 `elements.json`（元素库）。

常用参数：
```bat
python recorder.py --out my_flow.json          # 指定输出文件名
python recorder.py --url-filter "baidu.com"   # 多标签页时，只连含该网址的页（防误连）
python recorder.py --duration 30              # 录 30 秒自动停（后台/无终端场景）
python recorder.py --stop-file .apc_stop       # 出现 .apc_stop 文件即停（后台场景）
```

> **v3.3.0 关键修复**：以前在网页里点一下、跳到新页面再操作，**新页面的动作会丢**；现在录制器在页面跳转后自动重建监听通道，**整段流程一个不漏**。多步跳转的填报场景现在稳了。

### 5.2 对话式录制（chat_mode.py，最友好）

完全用大白话驱动，每一步还会弹窗让你确认是否记录：

```bat
python chat_mode.py "打开 https://www.baidu.com"   # 拉起自带浏览器并导航
python chat_mode.py "开始录制"                      # 开启带确认弹窗的交互录制
python chat_mode.py "停止"                          # 结束录制并落盘
python chat_mode.py "回放"                          # 回放最近一次录制
```

### 5.3 录制完怎么用

- 直接在 `scripts` 目录跑 `python main_task.py`（`task_flow.json` 默认就在这）：录制产物即点即播，自动获得**断点续跑、失败重试、后台运行**能力。
- 想改流程：直接编辑导出的 `task_flow.json`（增删步骤、改文本），再跑一次即可。
- **录制到的选择器现在基本都自动选"稳定写法"**：优先用输入框的 `placeholder`、按钮文字、`#id` 等语义属性，小白基本不用手动改；极少数复杂页面若回放报 NOT_FOUND，再用 Chrome "检查" 看元素的 `id` 改一下即可（见 FAQ Q2）。

### 5.4 桌面软件也能录

除了网页，还能录**本机任意 Windows 软件**（企业微信、微信、WPS、金蝶、用友、Excel 等）：

```bat
python desktop_recorder.py            # 回车开始，操作各类软件，回车停止
```

桌面录制以"屏幕坐标 + 前台窗口"定位，回放时尽量保持相同分辨率/窗口位置。

**网页 + 桌面一起录**（最常见真实场景，如"软件里复制 → 切到网页填报"）：

```bat
python record_session.py             # 同时录网页和桌面，按时间交错合并成统一 task_flow.json
```

## 6. 任务清单怎么写？（傻瓜版）

清单就是一个"步骤数组"，每一步写 3 样东西：

| 字段 | 填什么 | 说明 |
|------|--------|------|
| `type` | `browser` 或 `gui` | 浏览器操作 / 电脑桌面操作 |
| `func` | 动作名 | 想让它干什么（见下表） |
| `args` | 参数列表 | 动作需要的"材料"，用引号包起来 |

**常用"动作"速查表**

| 引擎 | 动作 func | args 示例 | 它干啥 |
|------|-----------|-----------|--------|
| browser | `open_url` | `["https://..."]` | 打开网页 |
| browser | `input_text` | `["#用户名框", "张三"]` | 往输入框打字 |
| browser | `click_elem` | `["#提交按钮"]` | 点网页上的按钮 |
| browser | `screenshot` | `["./截图.png"]` | 截图保存 |
| gui | `open_software` | `["C:\\Windows\\notepad.exe"]` | 打开软件 |
| gui | `click_icon` | `["按钮图.png", 0.8]` | 在屏幕上找图并点击 |
| gui | `hot_key` | `["ctrl","s"]` | 按组合键 |

💡 `#用户名框` 这种写法叫 CSS 选择器，就是网页元素的"身份证号"。不会找？用 Chrome 打开网页 → 右键点那个输入框 → "检查" → 在代码里看它的 `id="..."`，填 `#那个id` 就行。

**想玩本地演示页？** 把 `task_flow.json` 第一行换成你电脑上 `demo.html` 的真实路径，比如：

```json
{ "type": "browser", "func": "open_url", "args": ["file:///C:/Users/你/桌面/autopilot-composer/scripts/demo.html"] }
```

后面照旧：往 `#kw` 输入文字 → 点 `#go` → 截图，就能看到输入框和"已点击"的反馈，证明整套流程通了。

## 7. 进阶玩法：跳过点界面，直接"喊话"系统（T1 直连，v3.4.0 新增）

**先说人话**：以前机器人干"查订单"这种活，得打开网页 → 等加载 → 找输入框 → 填 → 点查询——跟你手动操作一模一样，界面一改版它就瞎。v3.4.0 起，如果这个系统有接口（API），机器人**直接调接口拿数据**，一步到位、毫秒级完成，界面怎么改都不影响它。

打个比方：以前是"假扮客户去前台排队问"，现在是"直接打内部电话问"。

### 7.1 你什么都不用做：录制时它自动偷学

最爽的地方来了——**v3.4.0 录制时，你在网页上每点一个按钮，录制器会同时记下两样东西**：

1. "点了哪个按钮"（界面动作，老办法，兜底用）；
2. "这一下其实触发了什么接口"（直连通道，新办法）。

回放时它**自动优先走接口**（日志里显示 `T1(call_api)`），接口万一失效了才退回"模拟点按钮"的老办法。**所以老用户直接升级就行，什么都不用改。**

### 7.2 手动用一把：三步直调一个接口

想跳过录制直接调接口查数据？三步：

**第 1 步**：在 `scripts` 里建 `api_registry.json`（接口登记表，照抄改网址就行）：

```json
{
  "version": 1,
  "templates": {
    "query_orders": {
      "method": "GET",
      "base_url": "https://api.example.com",
      "path": "/orders/list",
      "auth": { "type": "bearer" },
      "query_params": { "page": 1, "size": 20 },
      "assertions": [ { "type": "status", "expected": 200 } ]
    }
  }
}
```

**第 2 步**：把 Token 存进系统保险柜（**千万别写进任务清单！清单会被分享/入库**）：

```bat
cd scripts
.venv\Scripts\activate
python -c "from core.credential_manager import get_manager; get_manager().store('my_api_token', {'token': 'sk-你的token'}, backend='keyring')"
```

> 它存进了 Windows「凭据管理器」（控制面板 → 用户账户 → 凭据管理器 可查看），系统级加密。

**第 3 步**：清单里加一步，跑起来：

```json
[
  { "type": "api", "func": "call_api", "args": ["query_orders"],
    "credential_ref": "my_api_token" }
]
```

### 7.3 还能直接查数据库、指挥本地软件

- **查库**（`type: "sql"`）：适合"从数据库抽数存 Excel"这类活。有**四重保险**：① 只能参数化查询（防 SQL 注入）；② 可开"只读模式"（误发删除指令也伤不到库）；③ 手机号等敏感字段**自动打码**（`13********88`）；④ 每次执行都留痕到 `db_audit.log`。
- **指挥软件**（`type: "cli"`）：WPS/Excel/金蝶这类有 COM 接口的软件，可以不点界面直接"下指令"（如让 Excel 打开文件另存为），命令需先登记白名单，任务清单里只传参数——防止清单文件变成病毒。

这两个玩法稍微专业一点，**完整教程（含模板格式、安全规则、常见报错）看仓库里的 `docs/t1-direct-layer.md`**。

## 8. 想让它更稳？给关键步骤加个"检查点"（v3.4.1 新增）

**先说为什么**：以前它点完按钮就**默认成功了**。可有时候页面会弹个"用户名不能为空"，或者按钮其实是灰的、后端悄悄拒绝了——它照样往下跑，越跑越乱，最后在某一步莫名报错，你还得回头猜是哪一步出的问题。v3.4.1 起可以给关键步骤加个"检查点"，干完这步它**自己核对一遍**再往下走。

### 8.1 三步加一个检查点

在 `task_flow.json` 里那一步上加 `verify` 就行（照抄改字）：

```json
{
  "type": "browser",
  "func": "click_elem",
  "args": ["#保存按钮"],
  "verify": [ { "type": "text_present", "text": "保存成功" } ]
}
```

跑起来会多打印一行 `✔ 结果校验通过`；如果没通过，它会先重试，重试还不行就**停在断点**并告诉你原因，例如：

```
结果校验未通过：页面应包含文本「保存成功」（未命中）
```

### 8.2 最常用的三种写法

| 场景 | 写法 |
|------|------|
| 保存/提交后看提示 | `{ "type": "text_present", "text": "保存成功" }` |
| 登录后看网址变了没 | `{ "type": "url_contains", "value": "/dashboard" }` |
| 导出后看文件生成了没 | `{ "type": "file_exists", "path": "./导出.xlsx", "timeout": 30 }` |

还有 8 种（等遮罩消失、列表出现、弹窗关闭、金额核对、窗口标题、元素值比对等）见仓库 `docs/verify-and-health.md`，里面还有 9 个可直接复制的配方。

> 💡 **别给每一步都加**。只加在"有明确成功提示"的关键步骤（保存、提交、登录、导出、删除）上最划算——给每步都加，流程反而更容易被卡住。

### 8.3 元素库会自动"体检"

它现在会记住每个定位写法好不好用：

- 某个写法**连续错 5 次** → 自动弃用（以后不再浪费时间试它）；
- 某个元素**连续错 15 次** → 标记为"待复核"提醒你，但**不会删除**（万一只是页面慢呢）；
- 哪天界面又改回来了 → **一命中就自动复活**，你什么都不用做。

两条命令就够了：

```bat
python main_task.py --health        :: 看元素库体检报告
python main_task.py --health-reset  :: 人工复核后，恢复被弃用的元素
```

报告长这样：

```
元素库健康度：共 46 个元素，45 个正常，1 个待复核
（其中 40 个尚未回放过，不计入问题元素）

最需要关注的元素（按连续未命中排序）：
  - 按钮_保存（erp.internal）命中 3 次、连续未命中 16 次，定位器 2 个，已跳过策略：css
```

看懂两句话就行：**"尚未回放过"不算问题**（新录的还没跑过）；**"连续未命中"高的元素**说明界面真的改了，照着提示去更新它的定位器（方法见第 6 节）。

## 9. 它会自己琢磨「这一步有没有把握」（v3.5.0 新增）

先说个场景。你让一个新手司机送你回家，路不熟、天黑了、还下着雨——他多半**一脚油门一路往前冲**，
直到撞上什么才停下来。老司机不一样：会减速、会看导航、没把握就问一句"是走左边这条吗？"

**以前的机器人就是那个新手司机**：哪怕页面早就变了、登录早就过期了，它照样一步步往下跑，
不报错、不中断，到最后一步你才发现全错了。

v3.5.0 给它装上了老司机的判断力。

### 9.1 它凭什么判断？看这几个信号

| 它看什么 | 打个比方 | 怎么算分 |
|---|---|---|
| 走的是哪条路 | 高速 / 国道 / 乡间土路 | 直调接口（很稳）→ 点网页元素（较稳）→ 屏幕坐标（最脆弱） |
| 目标找得唯一吗 | "前方 200 米右转" vs "这附近有 3 个路口" | 只匹配到 1 个才放心；有 3 个都匹配 → 可能点错，扣分 |
| 这个目标以前好找吗 | 常走的路口 vs 总走错的路口 | 历史上稳定命中过 → 加分；最近连续找不到 → 扣分 |
| 这一步以前跑成过吗 | 熟路 vs 第一次走 | 用同一份清单的历史战绩来判断 |
| 有没有配"结果核对" | 到站签到 vs 到没到不知道 | 配了核对 → 加分（因为失败会被发现，不会假成功） |
| 导航有没有报错 | 前方封路 / 已偏离路线 | 登录页、错误页、意外弹窗 → **直接判极不可靠** |

合起来打一个 **0~5 分的"把握分"**：

- **4 分以上**：很有把握 → 照常干活
- **2.5 ~ 4 分**：还行 → 照常干活
- **1.2 ~ 2.5 分**：有点悬 → （开启后）**停下来问你**
- **不到 1.2 分**：太悬了 → 直接不干，让你处理

### 9.2 默认它不会打扰你（这点很重要）

装好之后默认是 **"观察模式"**：它照常自动化，**绝不弹窗、绝不打断**，
只是在每次跑完的结尾给你一句总结：

```
   置信度：平均 4.21/5（高 9 / 中 2 / 偏低 1 / 极低 0），人工介入 0 次
   ⚠️ 有 1 步置信度偏低，建议 `python main_task.py --health` 体检元素库
```

**为什么默认不拦你？** 因为拦错了比不拦更烦人。先用一两周，专门看它标出的"偏低"那几步
是不是真的值得担心——如果里面藏着"提交订单""删除数据"这种**做了就回不来**的动作，
那它就是实实在在在帮你兜底。

### 9.3 想让它"该问就问"？改一行就行

打开 `scripts/config.json`，找到 `"confidence"` 这一段：

```json
"confidence": {
  "mode": "shadow",
  ...
}
```

把 `shadow` 改成 `adaptive`，保存。之后再遇到没把握的步骤，会弹出这样一个窗口：

```
┌──────────────────────────────────────────────┐
│ AutoPilot 回放确认                            │
│ 置信 2.40/5 偏低 —— 这一步可能不可靠           │
│ 步骤 12/28：click_elem【登录】                 │
│ ┌──────────────────────────────────────────┐ │
│ │ [基准 4.2] T2 路径（浏览器元素定位）        │ │
│ │ [-1.80] 元素库里没有任何定位器能匹配到元素   │ │
│ │ [-0.30] 回退到内联选择器（元素库未生效）     │ │
│ └──────────────────────────────────────────┘ │
│           [继续执行] [跳过这步] [暂停任务]      │
│ 60 秒后自动选择：继续执行                       │
└──────────────────────────────────────────────┘
```

三个按钮对应三种心态：**"我确认没问题，继续" / "这步不重要，跳过" / "先停，我去看看"**。
不管你在不在电脑前都不会永久卡住——60 秒（可改）后自动选默认项；
而且**越危险的步骤默认越保守**（极不可靠的步骤连"继续执行"按钮都不给，默认暂停）。

窗口里那几行说明就是"为什么它觉得没把握"，照着看就知道该修哪里。

### 9.4 三个最容易踩的坑，它现在会自动识别

| 坑 | 有多坑 | 它怎么办 |
|---|---|---|
| **被踢回登录页** | **最阴险**：后面几十步全在登录页上空点，不报错，最后对账才发现全错 | 一眼认出来，明确告诉你"会话过期了，请人工登录后从断点续跑" |
| **冒出意外弹窗** | 弹窗挡住目标，结果点到了弹窗上 | 试着按 Esc、点关闭按钮帮你关掉 |
| **跳错页面 / 浏览器掉线** | 页面根本不对，后面全是白做 | 回退到上一个正常页面 / 重连调试通道 |

注意它**不会假装自己能搞定登录**——重登涉及你的密码，必须人来。这是刻意的：
**宁可诚实地说"需要你"，也不要假装成功把你带到坑里。**

### 9.5 重试也变聪明了

以前某一步失败，它会"等 2 秒 → 原样再跑一次 → 又失败 → 再原样跑"，三次机会纯粹浪费。

现在它会**换着法子试**：

```
     ↻ 换一种定位方式重试：「id」→「placeholder」
     ↻ 已重载页面并重新定位后重试
```

顺序还会看情况：**如果它已经知道是环境出问题，就先去修环境**
（在登录页上换个定位写法毫无意义）；环境正常时才先换定位写法（那多半是界面本身改了）。

### 9.6 建议的上线节奏

1. **第一周什么都不用改**，让它在观察模式跑，每次看结尾那行总结。
2. **检查"偏低"的步骤是不是该担心**——尤其有没有不可逆的操作。
3. **积累几天后改成 `adaptive`**，让它在该犹豫的时候问你。
4. 它标出来的问题，大多是元素库该更新了：跑 `python main_task.py --health` 照提示处理（见第 8 节）。

## 10. 录的时候它顺手帮你把检查点写好了（v3.6.0 新增）

### 10.1 先说以前有多别扭

第 8 节讲了"检查点"——关键步骤干完让它自己核对一下是不是真生效。这功能很好，但有个尴尬：
**规则得你自己一行行手写。**

而手写要求你回答一个问题：*"我点这个按钮之后，页面到底变成了什么样？"*

这个问题**录的时候你一眼就知道，事后过两天就想不起来了**。所以实际用下来，录了 60 步的流程，
能认真手写出检查点的通常不到 10 步——不是懒，是时机不对。

### 10.2 现在它自己记

录制的时候，它一边看你操作，一边顺手把"这一步让页面发生了什么变化"记下来，然后写成检查规则：

```bash
python record_session.py --verify auto     # 网页+桌面一起录（默认就是 auto，可以不加）
python recorder.py --verify auto           # 只录网页
```

录完你会看到多了一份 **`verify_candidates.md`**，里面逐条写着：

- 我给第几步加了什么检查
- 这个检查是**可靠**的（它会直接照做）还是**仅供参考**的（默认不加，除非你开 `--verify all`）
- **依据是什么**（比如"录制时回读确认字段值确实等于录入文本 C001"）
- **哪些地方我没加，为什么没加**（比如"这段提示里带订单号，下次跑肯定会变，所以不加")

最后一条其实同样重要——**不做的事也说清楚原因**，而不是悄悄略过。

### 10.3 它帮你自动加哪几种

| 它看到什么 | 就加什么检查 |
| --- | --- |
| 点完按钮，网址变了 | 检查网址里是否出现了新路径（例如 `/order/detail`） |
| 输入完，回读发现内容真的填进去了 | 检查这个框的值是否等于你输入的内容 |
| 点完冒出"保存成功"这类提示 | 检查页面上是否出现这段提示 |
| 点完弹出一个对话框 | 检查这个对话框是否出现（需 `--verify all`） |
| 点完页面从"加载中"变成加载完 | 检查加载动画是否消失（需 `--verify all`） |
| 桌面软件里的操作 | 检查当前前台窗口是不是那个软件（需 `--verify all`） |

其中**第二行"输入有没有真的填进去"最值钱**——这是自动化里最常见的静默失败：程序没报错，
但框其实是空的，一路跑到提交才炸。有了它，第一步就能发现。

### 10.4 它绝不乱加，两条硬规矩

**① 带数字的提示语一律不要。**
"订单 12345 已保存"这种提示里带订单号，第二次跑单号就变了，写进去必然误报。
所以它只认"保存成功""已提交审批"这类**每次都一样**的短句。

**② 密码、验证码、身份证、卡号，绝不记。**
这类输入它不会回读、也不会写进检查规则，免得你的凭证被存进流程文件里。

### 10.5 为什么默认只加"可靠"的那几种

因为**一条乱加的检查，比没有检查更糟**。

没加检查，最坏情况是"这步其实没成功，但它继续往下跑"——你事后能发现。
加错了检查，本来成功的流程会被它判成失败 → 重试 → 换做法 → 还是"失败" → 停下来等你。
你看到的是"昨天还好好的，今天第一步就挂了"，实际上页面一切正常，只是检查规则写得太乐观。

所以它默认只加**"录制时亲眼看到的事实"**（网址真的变了、值真的填进去了、提示真的出现了），
这些都几乎不可能误报。想更严格（弹窗、加载、窗口标题也检查），再加 `--verify all`。

### 10.6 录完之后怎么用

1. **打开 `verify_candidates.md` 扫一眼**——重点是"跳过的地方"，看看它为什么没检查那几步。
2. **直接跑** `python main_task.py`。加了检查的步骤，干完会自己核对。
3. **检查报错了怎么办**：
   - 如果报"页面应包含文本 X"而 X 里带了单号/日期 → 说明这条不该加，删掉它，或改成"等某个元素出现"；
   - 如果提示文案业务上确实改了 → 直接改 `task_flow.json` 里那句文案。
4. **不想要就在录的时候关掉**：`--verify off`（等于回到 v3.5.0 的行为，流程文件里不会多任何东西）。

> 想在自己电脑上确认这套东西真的工作：用调试模式开好 Chrome 后跑
> `python check_probe_live.py`，它会自己打开一个测试小页面点一遍给你看。
> 完整规则说明见仓库 `docs/recorder-verify-candidates.md`。

## 11. 中途出错了？不用慌

这个工具最贴心的地方：**它会记住跑到第几步**。比如第 30 步点了半天没反应，它会自动重试 `max_retry` 次（默认 3 次），还不行就停在那儿，并在 `breakpoint.json` 记下来。

✅ **修复后继续跑**：把问题解决了（网页加载慢就等等、选择器写错就改对），再执行一次 `python main_task.py`——它会**从第 30 步接着跑**，前面 29 步不用重来。

🔄 **想从头再来**：最省事的方式是加 `--reset` 参数重跑：`python main_task.py --reset` 会一键清空断点、从第 1 步开始；也可以手动删掉 `breakpoint.json` 文件，或把它里的 `current_step` 改成 `0`。

## 12. 小白最常问（FAQ）

**Q1：报 "403 Forbidden" / 连不上？**
99% 是启动 Chrome 时**漏了** `--remote-allow-origins=*`。回头看第 4 节步骤 3，照原样重新启动 Chrome（用 `browser_launcher.py` 则不用担心这个参数）。

**Q2：报 "NOT_FOUND"，点不到按钮？**
说明 `input_text` / `click_elem` 里的选择器不对。用 Chrome "检查" 重新看元素的 `id`。也可能需要等页面加载完（复杂的网页多等几秒）。

**Q3：`click_icon` 找不到图？**
截图要和运行时屏幕**一模一样**（分辨率、大小一致），否则认不出来。可以降低一点 `confidence`，但别低于 0.7。

**Q4：跑完我不想重头来？/ 想从头重跑？**
留着 `breakpoint.json` 就能续跑（同第 11 节）。想从头重跑最简单：加 `--reset` 参数 `python main_task.py --reset`，一键清空断点从第 1 步开始；也可手动删 `breakpoint.json`。

**Q5：想让它后台偷偷跑，不占屏幕？**
用本地版直接双击 `run_silent.bat`；或自己跑：`start /min pythonw scripts\main_task.py`。

**Q6：录制时跳到新页面后，后面的操作没录上？**
这是 v3.3.0 之前的老问题，现已修复。请确保你用的是 v3.3.0 的 `recorder.py`：页面跳转后监听通道会自动重建，整段流程一个不漏。若仍丢失，多半是页面在跳转瞬间你操作太快，稍等半秒即可。

**Q7：我开了好几个标签页，它连错了页？**
录制/回放时用 `--url-filter` 指定目标网址子串，例如 `--url-filter "baidu.com"`，只连含该网址的标签页，避免误连。

**Q8：想在服务器/后台自动录一段时间，没人敲回车？**
用非交互参数：`python recorder.py --duration 60`（录 60 秒自动停）或 `python recorder.py --stop-file .apc_stop`（出现该文件即停）。

**Q9：回放时我看不出它跑到哪一步 / 好像"没反应"？**
v3.3.2 起 `main_task.py` 会在屏幕**实时打印每一步进度**：`▶ 开始回放：共 N 步` → `✅ 步骤 1/N [动作 参数]` → … → 最后 `🎉 全部 N 步执行完成`。如果中途停在某一显示 ⚠️ 重试、❌ 暂停，按提示检查那一步（多半是页面没加载好或元素变了，见 Q2）；修好后直接 `python main_task.py` 从该步续跑，或加 `--reset` 从头跑。

**Q10：回放日志里有的步骤显示 `T1(call_api)`，是什么意思？（v3.4.0）**
恭喜，这一步走了"直连通道"——机器人没有模拟点界面，而是直接调了接口，速度从秒级变成毫秒级，界面改版也不影响它。如果哪天你反而想让它走界面（比如要截图留证据），把那一步的 `t1_ref` 字段删掉即可（见第 7 节和 `docs/t1-direct-layer.md`）。

**Q11：API 调用报"凭证未找到"？（v3.4.0）**
说明第 2 步的 Token 没存上，或换了 Windows 账户（凭据管理器按账户隔离）。重新执行一次 7.2 的第 2 步存储命令即可；用 `python -c "from core.credential_manager import get_manager; print(get_manager().get('my_api_token'))"` 可检查存没存上（打印出内容就是存上了）。

**Q12：它老说"置信度偏低"，是不是快要出事了？（v3.5.0）**
不是。置信度表达的是"这一步有多不确定"，不是"一定会失败"。但它确实指了方向——
偏低通常来自这几个原因：多个元素都匹配、走的是屏幕坐标、这步历史上失败过、元素连续找不到。
照日志里那几行说明去修（多半是更新元素库，见第 8 节），分数自然就上去了。

**Q13：我不想让它弹窗，怎么彻底关掉？（v3.5.0）**
默认本来就不会弹（观察模式，只提示不打断）。如果你想连评分都不要，把 `config.json` 里的
`confidence.enabled` 改成 `false`，行为就和 v3.4.x 完全一样。只想安静观察、不想看到提醒？
保持默认即可。

**Q14：它说"被跳转到登录页"，然后就不动了？（v3.5.0）**
这是它在保护你——会话过期后，后面几十步都会在登录页上白点，与其错到底不如停下来。
你手动登录一次，再执行 `python main_task.py` 从断点续跑即可。它不会替你自动重登，
因为那需要用到你的密码。

**Q15：它录的时候自动加的检查点，我还要自己改吗？（v3.6.0）**
多半不用。它只加"自己亲眼看到的事实"，而且默认只加最可靠的那几种（详见第 10 节）。
但有一条要留意：**如果哪天真报错了，先看报的是不是它自动加的那条**。
比如报"页面应包含文本 XX"，而那段文案里带了单号或当天日期 → 说明这条本就不该自动加，
删掉它、或改成"等某个元素出现"就好（改法见第 8.2 节）。**删掉自动加的检查点，绝不会影响流程本身**。

**Q16：为什么有的步骤它没给我加检查点？是漏了吗？（v3.6.0）**
不是漏，是它**故意不加**。两种情况它一定不加：
① 拿不准——比如输入框里的内容它读回来和你写的不一样（富文本编辑器、带自动格式化的输入框经常这样），宁可空着也不瞎写；
② 敏感——密码、验证码、身份证、银行卡这类，它**根本不会去读回值**（怕把你的密码写进文件里）。
想知道每一步具体加了什么、跳过了什么、为什么，打开录制生成的 `verify_candidates.md`，它逐条写清楚了。
跳过的那些想要也行，照第 8.1 节手写一个，两种方式可以混着用。

**Q17：它报 502 Bad Gateway / 说"连不上调试端口"，但我 Chrome 明明开着？（v3.6.0）**
你电脑上大概率装过代理软件（Clash / v2ray / 公司 VPN 之类），它把"本机地址"也走了代理，
于是本机内部的连接被绕了一圈、返回 502。**v3.6.0 已经修好，正常情况你不用管了**。
如果还是遇到（比如某个旧脚本），临时救急的办法是先关掉代理软件再跑；
排查时可在命令行输入 `set HTTP_PROXY=` 清掉再启动机器人。**注意：只有"连本机浏览器"这条通道绕过了代理，
真正去调外部接口的 T1 直连（第 7 节）仍然照常走代理**——这是故意的，否则公司内网接口会连不上。

## 13. 安全提醒（必看）

- [ ] 它会真实操控你的鼠标、键盘、浏览器——**只在你自己的电脑和测试账号上用**。
- [ ] 跑之前**保存好手头工作**，避免它不小心点到别的地方。
- [ ] 别拿去操作他人账号或生产系统。
- [ ] 桌面图标识别靠"像素比对"，换显示器/缩放比例后可能失效。
- [ ] 录制到的文件（含截图）会落在本地 `scripts/recordings/` 下，注意别误存敏感信息。
